const { Service } = require('feathers-nedb')

exports.UsersInfos = class UsersInfos extends Service {
  /* constructor(options, app) {
    super(options);

    app.get('mongoClient').then(db => {
      this.Model = db.collection('users-infos');
    });
  } */
}
